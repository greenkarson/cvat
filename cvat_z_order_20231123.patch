diff --git a/Dockerfile_stmf.ui b/Dockerfile_stmf.ui
new file mode 100755
index 000000000..30bf27b68
--- /dev/null
+++ b/Dockerfile_stmf.ui
@@ -0,0 +1,35 @@
+FROM node:lts-slim AS cvat-ui
+
+ARG WA_PAGE_VIEW_HIT
+ARG UI_APP_CONFIG
+ARG CLIENT_PLUGINS
+
+ENV TERM=xterm \
+    LANG='C.UTF-8'  \
+    LC_ALL='C.UTF-8'
+
+# Install dependencies
+COPY package.json /tmp/
+COPY yarn.lock /tmp/
+COPY cvat-core/package.json /tmp/cvat-core/
+COPY cvat-canvas/package.json /tmp/cvat-canvas/
+COPY cvat-canvas3d/package.json /tmp/cvat-canvas3d/
+COPY cvat-ui/package.json /tmp/cvat-ui/
+COPY cvat-data/package.json /tmp/cvat-data/
+
+# Install common dependencies
+WORKDIR /tmp/
+RUN yarn install --ignore-scripts --frozen-lockfile
+
+# Build source code
+COPY cvat-data/ /tmp/cvat-data/
+COPY cvat-core/ /tmp/cvat-core/
+COPY cvat-canvas3d/ /tmp/cvat-canvas3d/
+COPY cvat-canvas/ /tmp/cvat-canvas/
+COPY cvat-ui/ /tmp/cvat-ui/
+RUN CLIENT_PLUGINS="${CLIENT_PLUGINS}" UI_APP_CONFIG="${UI_APP_CONFIG}" yarn run build:cvat-ui
+
+FROM nginx:mainline-alpine
+# Replace default.conf configuration to remove unnecessary rules
+COPY cvat-ui/react_nginx.conf /etc/nginx/conf.d/default.conf
+COPY --from=cvat-ui /tmp/cvat-ui/dist /usr/share/nginx/html/
diff --git a/cvat-ui/src/containers/annotation-page/standard-workspace/objects-side-bar/object-item.tsx b/cvat-ui/src/containers/annotation-page/standard-workspace/objects-side-bar/object-item.tsx
old mode 100644
new mode 100755
index be9817ee6..6c3bd49cb
--- a/cvat-ui/src/containers/annotation-page/standard-workspace/objects-side-bar/object-item.tsx
+++ b/cvat-ui/src/containers/annotation-page/standard-workspace/objects-side-bar/object-item.tsx
@@ -237,7 +237,9 @@ class ObjectItemContainer extends React.PureComponent<Props, State> {
         const { objectState, readonly, minZLayer } = this.props;
 
         if (!readonly) {
-            objectState.zOrder = minZLayer - 1;
+            // objectState.zOrder = minZLayer - 1;
+            // test code
+            objectState.zOrder = objectState.zOrder - 1;
             this.commit();
         }
     };
@@ -246,7 +248,9 @@ class ObjectItemContainer extends React.PureComponent<Props, State> {
         const { objectState, readonly, maxZLayer } = this.props;
 
         if (!readonly) {
-            objectState.zOrder = maxZLayer + 1;
+            // objectState.zOrder = maxZLayer + 1;
+            // test code
+            objectState.zOrder = objectState.zOrder + 1;
             this.commit();
         }
     };
diff --git a/cvat-ui/src/containers/annotation-page/standard-workspace/objects-side-bar/objects-list.tsx b/cvat-ui/src/containers/annotation-page/standard-workspace/objects-side-bar/objects-list.tsx
old mode 100644
new mode 100755
index a1552a75c..b5514289d
--- a/cvat-ui/src/containers/annotation-page/standard-workspace/objects-side-bar/objects-list.tsx
+++ b/cvat-ui/src/containers/annotation-page/standard-workspace/objects-side-bar/objects-list.tsx
@@ -480,7 +480,9 @@ class ObjectsListContainer extends React.PureComponent<Props, State> {
                 preventDefault(event);
                 const state = activatedState();
                 if (state && !readonly && state.objectType !== ObjectType.TAG) {
-                    state.zOrder = minZLayer - 1;
+                    // state.zOrder = minZLayer - 1;
+                    // test code
+                    state.zOrder = state.zOrder - 1
                     updateAnnotations([state]);
                 }
             },
@@ -488,7 +490,9 @@ class ObjectsListContainer extends React.PureComponent<Props, State> {
                 preventDefault(event);
                 const state = activatedState();
                 if (state && !readonly && state.objectType !== ObjectType.TAG) {
-                    state.zOrder = maxZLayer + 1;
+                    // state.zOrder = maxZLayer + 1;
+                    // test code
+                    state.zOrder = state.zOrder + 1;
                     updateAnnotations([state]);
                 }
             },
diff --git a/docker-compose.yml b/docker-compose.yml
old mode 100644
new mode 100755
index 0bf34777f..fe96b684a
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -227,7 +227,8 @@ services:
 
   cvat_ui:
     container_name: cvat_ui
-    image: cvat/ui:${CVAT_VERSION:-v2.5.2}
+    # image: cvat/ui:${CVAT_VERSION:-v2.5.2}
+    image: cvat-ui-stmf
     restart: always
     depends_on:
       - cvat_server
diff --git a/start_cvat.sh b/start_cvat.sh
new file mode 100755
index 000000000..4154125c9
--- /dev/null
+++ b/start_cvat.sh
@@ -0,0 +1,3 @@
+export CVAT_HOST=192.168.100.110
+export CVAT_VERSION=v2.5.2
+docker compose -f docker-compose.yml -f docker-compose.override.yml up -d
diff --git a/stop_cvat.sh b/stop_cvat.sh
new file mode 100755
index 000000000..8c450dd04
--- /dev/null
+++ b/stop_cvat.sh
@@ -0,0 +1 @@
+docker compose -f docker-compose.yml -f docker-compose.override.yml stop
